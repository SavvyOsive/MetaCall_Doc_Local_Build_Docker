Quickstart
==============

The easiest way to install MetaCall is the following:

.. code-block:: bash

    curl -sL https://raw.githubusercontent.com/metacall/install/master/install.sh | sh



For more information about other install methodologies and platforms or Docker, :ref:`check the install documentation <installation>`.
